# LedgerLine Core — environment variables (fill in locally, never commit .env)

# Robinhood Chain testnet (verify against official docs before use)
ROBINHOOD_CHAIN_RPC_URL=
ROBINHOOD_CHAIN_CHAIN_ID=46630

# Deployer key — store locally only, never paste into chat or commit
DEPLOYER_PRIVATE_KEY=

# Explorer verification (if/when supported for the target chain)
EXPLORER_API_KEY=

# Frontend
NEXT_PUBLIC_RPC_URL=
